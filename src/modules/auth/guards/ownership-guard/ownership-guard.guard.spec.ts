import { OwnershipGuardGuard } from './ownership-guard.guard';

describe('OwnershipGuardGuard', () => {
  it('should be defined', () => {
    expect(new OwnershipGuardGuard()).toBeDefined();
  });
});
